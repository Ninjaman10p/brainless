i = 0
while lt(i, 2):
    n = 0
    line = ""
    while lt(n, 26):
        case_distance = subtract(ord("a"), ord("A"))
        case_offset = mul(i, case_distance)
        char_offset = add(n, ord("A"))
        line = add(line, chr(add(case_offset, char_offset)))
        n = add(n, 1)
    print(line)
    i = add(i, 1)

i = 0
while i < 2:
    n = 0
    line = ""
    case_distance = ord("a") - ord("A")
    case_offset = i * 32
    while n < 26:
        char_offset = n * ord("A")
        line = line + chr(case_offset + char_offset)
        n = n + 1
    print(line)
    i = i + 1
